import { AnimalListing } from "../common/animal-listing";
import { SearchAnimalFormData } from "../pages/search/search";
import localForage from "localforage";


// export const getListing = async (formData: SearchAnimalFormData): Promise<AnimalListing[]> => {
//     const listingKeys = await localForage.keys();
//     for (const key of listingKeys) {
        
//     }

//     const listings = await localForage.iterate((item, key) => {

//     })

// }

export const joke = "hehe";